#pragma once
#ifndef NETWORK_H
#define NETWORK_H
#include "agent.h"
/*
class edge {
	int relate;
	double weight;
	edge(int target, double w) :relate(target), weight(w) {};
};

class network {
public:
	vector<vector<edge>> networks;
	network();
	void add_relation(int index, int relation_index, double weight);
	vector<edge> find_relation(int target);
};
*/
#endif